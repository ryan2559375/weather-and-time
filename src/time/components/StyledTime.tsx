import styled from "styled-components";
import { StyledWeatherDefinition } from "../../weather/components/StyledWeather.tsx";

export const StyledTime = styled(StyledWeatherDefinition)`
  grid-area: time;
  font-size: 120%;
  align-self: flex-end;
`;
